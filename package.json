{
  "dependencies": {
    "@types/multer": "^1.4.7",
    "cypress": "^8.4.1",
    "dotenv": "^10.0.0",
    "multer": "^1.4.3"
  },
  "scripts": {
    "cypress": "cypress open",
    "heroku-start-client": "cd client && yarn heroku-production",
    "heroku-start-server": "cd server && yarn heroku-production"
  }
}
